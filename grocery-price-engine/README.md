# Grocery Price Engine

Daily, HTTP-first price pulls with cache fallback.

## Run once
npm run run:once

## Run cron (daily at 7:00 AM)
npm run run:cron

## Config
- config/stores.json
- config/items.json

## Output
- data/latest.json
- data/latest.csv
- data/cache.json
